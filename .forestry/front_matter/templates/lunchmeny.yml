---
label: Lunchmeny
hide_body: true
fields:
- name: monday
  type: field_group_list
  fields:
  - name: ratt
    type: list
    config:
      use_select: false
      min: 
      max: 
    label: Rätt
  config:
    min: 
    max: 
    labelField: 
  label: Måndag
